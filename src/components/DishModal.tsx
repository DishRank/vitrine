'use client';
import { useTranslations } from 'next-intl';
import { useState, useEffect } from 'react';

// This is a placeholder - the modal will be triggered by dish card clicks
// For now it's a client-side component that can be enhanced later
export default function DishModal() {
  return null; // Will be implemented when dish cards are clickable
}
