/** Message de remerciement par défaut — DOIT rester identique au fallback SQL
 *  de `auto_thank_reviews()` (migration 107) pour que l'aperçu web et l'envoi
 *  auto affichent le même texte. */
export const DEFAULT_THANK = 'Merci beaucoup pour votre avis, au plaisir de vous accueillir à nouveau !';
